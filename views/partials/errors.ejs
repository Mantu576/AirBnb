<% if (errors && errors.length > 0) { %>
    <div class="bg-red-100 text-red-700 p-3 rounded mb-4">
      <ul>
        <% errors.forEach(error => { %>
          <li><%= error.msg %></li>
        <% }); %>
      </ul>
    </div>
  <% } %>
  